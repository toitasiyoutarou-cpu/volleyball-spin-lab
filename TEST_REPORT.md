# 検証結果 — SPIN LAB v1.2

実施日：2026-09-15

**実写動画・iPad Safari実機・ブラウザの動画デコーダーは未検証です。以下は合成画像、模擬DOM＋ネイティブCanvas、FFmpegによるMP4復号の検証です。**

## v1.2の結果

| 検証 | 結果 |
|---|---|
| 不均等時刻の合成画像200枚 | 正解5Hz・60°に対し 4.99936Hz・59.45°。VFRの実時刻による周期検証を通過。 |
| VFRを符号化したMP4 | 256×256px、240枚、球の直径144px、正解5Hz・60°。MP4の時刻表からVFRを検出し、FFmpeg復号画像と実装の推定器で 4.99970Hz・60.20°。品質スコア：Hz 61%、軸 62%。 |
| 回転速度が変化する合成画像 | 1.8Hzから約8.8Hzへ変化、軸は60°。一定回転のHzは保留し、3時間区間で方向が一致する軸を 59.64°として採用。 |
| 外周検出がF60〜62で連続失敗 | 後続フレームの輪郭から時刻補間し、3枚とも自動復帰。追加の位置保存なしで144枚・143ペアを処理。5.00019Hz・60.14°。 |
| 近傍再探索 | 通常の予測位置で外周検出に失敗しても、周辺候補と模様の順方向・逆方向照合で復帰。 |
| 長い追跡途切れ・無地画像 | 自動でフレームを飛ばさず停止。元の失敗フレームを表示し、手動修正・区間変更が可能。 |
| VFRのコントローラー統合 | 実時刻に応じて画像・ボール位置を変化させ、4.99779Hz・60.15°。VFRの一律拒否を撤廃し、確認済み時刻・追跡の条件を維持。 |
| 時刻の重複・倍率の誤用 | 非増加の提示時刻を棄却。VFRに撮影FPS÷保存FPSのスロー倍率を適用する操作を阻止。 |
| 従来の手動復帰 | 自動復帰をオフにした10項目が引き続き通過。同一フレーム候補・再開・重複防止・短区間の案内を維持。 |
| 従来の数値採否 | 既知回転、停止、無模様、短区間、奥行き軸、対称模様、強いぼけ、角度規約が通過。 |

## 精度の改善と限界

- 球面の中心・直径を固定する補助フィット、複数の画像間隔、速度から独立した軸の時間的一致判定を追加しました。モデルを識別できない画像から無条件に角度を出す変更ではありません。
- 低速0.5Hzの短い合成区間では、別間隔の照合で軸の内部推定が約59.02°になっても品質スコア不足で表示を保留しました。正解に近い内部値が出たことと採用できることを区別します。
- 直径96pxの圧縮VFR試作映像ではHzが品質スコア不足となりました。直径144pxの同梱映像では通過しました。小さいボール・圧縮・模様による条件依存が残っています。
- 自動復帰のテストは外周スコアの不足と無地画像を制御して再現しました。体育館背景や他の丸い物体、手やネットによる実写の遮蔽に対する検出率を測定したものではありません。
- VFRのMP4試験はFFmpegで復号しました。Safariのseek精度やrequestVideoFrameCallbackを実機で検証したことにはなりません。
- 信頼度は未校正の品質スコアです。正解確率、研究上の誤差保証ではありません。

## 再実行

```bash
node tests/test-engine.cjs
node tests/test-v12.cjs
node tests/test-auto.cjs
node tests/test-recovery.cjs
node tests/test-vfr-video.cjs
```

自動復帰・手動復帰のコントローラー試験には `@napi-rs/canvas`、VFR MP4の生成・復号試験にはFFmpegを使います。アプリの利用にはどちらも不要です。

実機では、まず同梱の `synthetic-vfr.mp4` を読み込み、VFR表示と正しいフレーム送りを確認してください。中心128,128・直径144px・開始0・終了239・実時間再生を設定し、円の目視確認後に結果を確認します。その後で元の実写動画を使ってください。

---

## 以前の検証結果 — SPIN LAB v1.1

実施日：2026-09-15

**v1.1のエラー復帰はNode.js・ネイティブCanvas・模擬DOMで検証しました。実ブラウザ、実写240fps動画、iPad Safari実機のテストではありません。**

## v1.1で実施した復帰テスト

`node tests/test-recovery.cjs` の10項目を通過しました。実際のコントローラー・ボタン処理・推定器を使い、外周スコア不足、模様の欠落、読み出し失敗を制御して再現しています。タッチはPointerイベントの模擬入力です。

| 検証 | 結果 |
|---|---|
| F60で外周の判定に失敗 | 表示フレームもF60になり、位置編集が可能。処理済み60枚を保持。条件未変更の再クリックでは推定器を呼ばない。 |
| 同一フレームの複数候補 | 別々の中心を保存し、両方の候補が残る。リストの「使用する」で1候補を選択できる。 |
| 手動の円で再開 | 外周自動判定が失敗する条件でも、保存した円をそのまま使う。処理済みのデータを保持し、最終144枚・143ペアで重複なし。 |
| 模様不足で停止・再開 | F37に一様画像を入力して棄却。棄却した画像は推定器の履歴に追加しない。手動でも失敗した場合の具体的な対処を表示し、同一の手動指定での再計算を抑制。修正後は144枚・143ペアに復帰。 |
| 過去フレームの候補を変更 | 処理済み部分を破棄し、開始から再解析する状態に戻る。 |
| 補間 | 端点が足りなければ該当フレームへ誘導。位置保存後は外周探索を呼ばず、選択した円の間を補間。 |
| 手動指定の品質条件・削除 | 40px未満・画面外の円は引き続き棄却。候補を1つずつ削除でき、使用中を削除した場合は同フレームの残る候補へ切り替わる。 |
| 区間の短縮 | 停止直前を終了に設定。残り48枚未満なら開始変更や別区間の選択を案内。 |
| 読み出し失敗と一時停止 | 古いプレビューを誤って保存できない。読み出し回復後に再開可能。中止後の再開も処理済みデータを保持。 |
| 表示・通信の制約 | 時間・追跡確認の条件を維持。外部通信APIなし、CSPの通信禁止を維持。 |

復帰後の合成144フレームでは、**5.00018 Hz / 60.12°**（正解5Hz・60°）でした。これは回帰確認の結果であり、実写の精度保証ではありません。

## 継続して確認した数値処理

`node tests/test-engine.cjs` をv1.1でも実行し、既知回転、停止、無模様、短区間、奥行き軸、対称模様、角度規約、強いぼけの採否判定を通過しました。

内蔵デモ360フレームのコントローラー処理も再実行し、端点2つ指定で5.00004Hz・60.12°、初期位置のみで5.00044Hz・60.20°でした。

HTMLはインラインJSの構文、IDの重複、コントロール参照を検査しました。実ブラウザのレイアウトやSafariのフレーム取得は未検証です。v1.0のMP4時刻表・FFmpeg復号試験は下記に履歴として残し、今回の実ブラウザ試験としては扱いません。

## 開発者向け再実行

```bash
node tests/test-engine.cjs
node tests/test-recovery.cjs
```

復帰試験はNode.jsと `@napi-rs/canvas` を使用します。アプリ本体・GitHub公開にはいずれも不要です。結果は `tests/recovery-results.json` に保存します。

## v1.1の実機確認

1. ページ上部が「研究・実験用 v1.1」になっていることを確認する。
2. 同じフレームに2つの候補を追加し、「使用する」で切り替えられることを確認する。
3. 実写で停止したフレームと表示番号を照合し、中心・直径を修正して「この位置を保存して再開」を押す。
4. 外周が弱い区間では補間モードを試し、フレームを送って円がボール全体に合っているか確認する。
5. 模様が見えない画像では数値を無理に出さず、具体的な対処が表示されることを確認する。
6. CSVの `tracking_method` と `candidate_id`、時刻確認、採否理由を確認する。

---

## 以前の検証結果 — SPIN LAB v1.0

実施日：2026-09-14

**合成データで数値処理・時刻表・追跡・採否判定を検証しました。実写の精度検証、Chrome上の実動画seek、iPad Safari実機検証は未実施です。**

## 実施した検証

| 検証 | 入力・条件 | 結果 |
|---|---|---|
| 数値推定 | 独立した球面描画式、240fps相当、5Hz・60°、144枚 | 5.00004Hz / 59.66°。品質条件通過。 |
| MP4のメタデータ | H.264、640×360、240fps、360枚、1.5秒、Bフレームを含むMP4 | FPS・フレーム数・ctts・elst補正後の提示時刻を正しく読めた。 |
| 復号したMP4画像 | 上記MP4の最初の240枚をFFmpegで復号。端点に基づく位置予測＋実装の外周追跡＋96px切り出し＋実装の回転推定 | 5.00001Hz / 59.80°。239/239ペア採用。中心誤差平均0.84px（この合成試験のみ）。 |
| アプリ処理の統合 | 模擬DOM・ネイティブCanvas・Workerなしのフォールバック経路。内蔵合成デモ360枚、端点2つ指定 | 5.00004Hz / 60.12°。追跡・解析・結果表示ロジックを通過。 |
| 初期位置のみの追跡 | 同じ合成デモ360枚、フレーム0の位置だけ指定 | 5.00044Hz / 60.20°。最後まで追跡し、品質条件通過。 |
| 停止 | 模様あり、回転0、64枚 | 軸とHzを棄却。 |
| 模様なし | 一様な球面色と明暗、64枚 | 回転の結果を棄却。 |
| 区間不足 | 5Hz、48枚（約0.196秒） | Hzを棄却。軸は別に判定。 |
| 奥行き方向の軸 | 回転ベクトルz成分0.998、144枚 | 画面上の軸角を棄却。Hzは別に判定。 |
| 繰り返し模様 | カメラ向きの6回対称模様 | 同等の短周期を検出し、Hzを棄却。 |
| 強いぼけ | 反復平滑化した画像 | 鮮鋭度の基準で棄却。 |
| 角度規約 | 軸0°、135°、179° | 1.48°、133.81°、178.06°。180°周期で比較。これは単一ペアの試験。 |
| 採否の必須条件 | 時間未確認、時刻表未確認、提示フレーム時刻不足、追跡未確認 | 数値の表示を保留。 |
| 構文・通信 | インラインJS、コード内APIとCSPを検査 | 構文エラーなし。fetch/XHR/WebSocket/sendBeaconなし。外部依存の読み込みなし。 |
| HTML保存 | 初期HTMLの保存経路を検査 | 実行中の動画Blob URLや解析値を埋め込まない。 |

品質スコアの数値は正解確率ではありません。MP4復号試験の軸・Hzの品質スコアは各57%、内蔵デモの統合試験は各61%でした。測定誤差の保証値に読み替えないでください。

## 検証の範囲

- 数値試験では、推定器に正解のHzや角度は渡していません。
- MP4は独立したPythonの球面描画とFFmpegで生成しました。実写ではありません。
- MP4復号試験のフレーム取得はFFmpegです。**HTMLVideoElementのフレーム取得を試験したものではありません。**
- アプリ統合試験は模擬DOMとネイティブCanvasを使用しました。実ブラウザでのレイアウト、タッチ入力、Worker、ダウンロードの保証にはなりません。
- この環境ではブラウザのローカルHTMLへの直接アクセスが安全制限で拒否されたため、ブラウザによる動作確認の代替として上記を実施しました。
- 任意のバレーボール模様、選手・ネット・体育館背景、実際のモーションブラー、遠近変化についての精度を検証したものではありません。

## 実機で最初に確認すること

1. 同梱の `synthetic-240fps.mp4` を読み込み、240fps・360フレーム・1.5秒と表示されること。
2. ±1フレームの操作で模様が少しずつ移動し、フレームが止まったままにならないこと。
3. 開始フレーム0で位置を指定し、必要なら終了フレーム359にも位置を指定して解析すること。
4. 追跡確認後、値が5Hz・60°付近になり、CSVの提示時刻確認が十分に成立していること。
5. 中止ボタン、位置の再指定、CSV保存が動くこと。
6. その後、未編集の実写240fps動画で追跡円と周期を目視照合すること。
7. 実写について既知の回転条件、または第三者の手作業計測を使い、角度誤差・Hz誤差・棄却率・誤採用率を評価すること。

上記の実機確認でフレーム時刻が一致しなければ、値を採用しない仕様です。Safariのバージョンと元動画の情報を記録し、読み出し経路を評価してください。

## 数値試験の再実行

```bash
node tests/test-engine.cjs
node tests/test-video.cjs
```

2つ目はFFmpegが必要です。アプリの利用やGitHub公開にはNode.js・Python・FFmpegは不要です。
